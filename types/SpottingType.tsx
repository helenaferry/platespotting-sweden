export type SpottingType = {
    id: Number,
    created_at: string,
    plateNumber: number,
    location_lat: string,
    location_lng: string,
    location_txt: string,
    dateSpotted: string,
    note: string,
    email: string
}